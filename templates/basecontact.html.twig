<!DOCTYPE html>
<html>
     <head>
        <meta charset="UTF-8">
        <title>{% block title %}NoteBo0k{% endblock %}</title>
        {% block stylesheets %}
            <link href="{{ asset('css/bootstrap.min.css') }}" rel="stylesheet" />
            <link href="{{ asset('css/style.css') }}" rel="stylesheet" />
        {% endblock %}
    </head>
    <body>
        <nav class="navbar navbar-expand-lg navbar-light bg-light row">
            <a class="navbar-brand textenav" href="#">NoteBo0k++</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarColor03">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('listeRep') }}">Répertoires</a>
                    </li>
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('listeContacts', {'id': repertoire.id }) }}">Contacts<span class="sr-only">(current)</span></a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('gestionContact', {'id': repertoire.id }) }}">Gérer Contacts</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('ajoutContact', {'id': repertoire.id }) }}">Ajouter Contact</a>
                    </li>
                </ul>
            </div>
        </nav>
        {% block body %} {% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
